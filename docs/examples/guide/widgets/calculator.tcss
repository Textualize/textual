Counter {
    background: $panel-darken-1;
    padding: 1 2;
    color: $text-muted;

    &:focus {
        background: $primary;
        color: $text;
        text-style: bold;
        outline-left: thick $accent;
    }
}
